[project]
name = "song-patch"
version = "0.1.0"
description = "Lambda function for PATCH /songs/{id} endpoint"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.35.0",
    "lit-up-api-models",
]


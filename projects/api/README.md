# Lit Up API

This one is in a weird state. In the future these capabilities will be encapsulated in an API. For now they're just python scripts that can be run via the `taskfile.yaml`.

To be refactored & fleshed out soon!
